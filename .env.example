# Supabase — copy to .env.local and fill in from your Supabase project settings.
# Leave these blank to run the app in demo mode (simulated auth, no backend).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
